{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'Static portfolio deployment - no dependencies to install'",
  "buildCommand": "echo 'Using app/static as the deployment output'",
  "outputDirectory": "app/static",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/",
      "destination": "/portfolio"
    },
    {
      "source": "/assets/:path*",
      "destination": "/:path*"
    }
  ]
}
